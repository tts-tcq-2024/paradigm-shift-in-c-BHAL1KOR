#include "WarningConfig.h"

// Default configuration, can be edited for tuning
// ENABLE (1) means warnings are enabled, DISABLE (0) means warnings are disabled

WarningConfig warningConfig = {ENABLE, ENABLE, ENABLE};
